
import 'package:flutter/material.dart';

class CardGroupScreen extends StatelessWidget {
  const CardGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
